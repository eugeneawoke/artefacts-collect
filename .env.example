# Vercel Postgres (from Vercel dashboard → Storage)
POSTGRES_URL=

# Web Push VAPID keys — generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Same public key, exposed to browser for push subscription
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
